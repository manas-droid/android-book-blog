mutation AddPostCommentResult($postId : Int! $parentId:Int $comment : String!){
    addPostComment(postId  : $postId parentId:$parentId comment : $comment){
        ok
        errors{
            path
            message
        }
    }
}